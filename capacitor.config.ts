import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alphadeltastudios.simplereminders',
  appName: 'Simple Reminders',
  webDir: 'dist'
};

export default config;
